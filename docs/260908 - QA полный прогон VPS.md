# QA полный прогон — VPS

| Поле | Значение |
|------|----------|
| **Дата прогона** | 2026-09-08 |
| **Задание** | [260910 - задание QA полный прогон](260910%20-%20задание%20QA%20полный%20прогон.md) |
| **Стенд** | http://195.19.209.121/ (`DEMO_MODE`) |
| **Сравнение** | Локальный отчёт: [260908 - QA полный прогон отчёт](260908%20-%20QA%20полный%20прогон%20отчёт.md) |
| **Как** | Те же HTTP-сценарии, что локально; без `manage.py test` / shell |

## Вердикт

**Можно показывать на VPS.** Золотой путь и фичи 02–08.09 прошли. Локальный блокер «директор: В комнату → 403» **на этом стенде не воспроизводится** (кнопки у директора нет).

Перед показом желательно: `python manage.py seed_demo_scenario` — сейчас у `director@` нет комнаты «Демо для стейкхолдеров».

---

## Чеклист закрытия (§8)

- [x] Новый директор: мастер → оплата → слот  
- [x] Три роли, один проект, разные вкладки  
- [x] Задача → отчёт → Quality Gate → начисление  
- [x] Лид: чеклист сохраняется, Hot только тимлид  
- [x] Менеджер: inbox 200, без шапки комнаты  
- [x] Колокольчик: бейдж = разговоры; DT скрыт у фрилансера; менеджер без 🔔; XSS экранирован  
- [ ] Автотест колокольчика — **n/a на VPS**  
- [x] Отчёт за период из комнаты, дефолт = эта комната; на `/` — «Все проекты»  
- [x] Расторжение: форма, модалка, список, протест, отзыв  
- [x] Team seats на Обзоре  
- [x] Материалы открываются  
- [ ] `python manage.py test` — **n/a на VPS**  

Итог скрипта: **76 OK**, findings: **2 high**, blockers: **0**.

---

## Находки

| # | Severity | Роль / место | Факт |
|---|----------|--------------|------|
| H1 | high | `director@` дашборд | Нет проекта «Демо для стейкхолдеров»; есть «Директоров Cold Calling Machine» (`14187050-…`). Сид, похоже, не гоняли |
| H2 | high | Тимлид → Новая задача | В исполнители попадает **директор** (как локально) |

### Не воспроизвелось vs локальный прогон

| Локально | На VPS |
|----------|--------|
| Директор видит «В комнату», POST 403 | У директора **нет** кнопки «В комнату» / `add-to-room` |
| Новый FR верифицировали через `manage.py shell` | На VPS FR виден с `show_unverified=1`, тимлид добавил через Команду без shell |

503 после ретраев в этом прогоне не зафиксированы.

---

## Следы на стенде

| Что | Значение |
|-----|----------|
| Директор прогона | `qa.full.dir.09082325@test.local` / `QaFull2026!` |
| Фрилансер | `qa.full.fr.09082325@test.local` / `QaFull2026!` |
| Демо-пароль | `DemoPass123!` |
| Существующий проект директора | `14187050-4a6b-4b92-9283-0ea8f2c9e2f1` |

---

## Как вести демо на VPS

1. Подбор — **тимлидом** (у директора кнопки нет — ок).  
2. Задачи — фрилансеру, не директору из селекта.  
3. Для сценария «Демо для стейкхолдеров» — сначала `seed_demo_scenario` на сервере или показывать комнату нового директора.  
4. Расторжение / колокольчик / Hot → менеджер — можно показывать.

# QA-прогон на демо-VPS

| Поле | Значение |
|--------|----------|
| **Дата** | 2026-09-02, ~22:30 |
| **Среда** | http://195.19.209.121/ (DEMO_MODE, ссылка активации на экране) |
| **Кто** | Ручной прогон от лица пользователей, без правок кода |
| **Связано** | [локальный прогон](260902%20-%20QA%20прогон%20перед%20демо%20стейкхолдерам.md), [DEMO_SCRIPT](DEMO_SCRIPT.md) |

**Зачем:** проверить живой сайт так, как будут заходить стейкхолдеры: роли, комнаты, задачи, лиды, inbox.

---

## Вердикт

Золотой путь на VPS **проходит**: регистрация → мастер → оплата-stub → тимлид → фрилансер → задача / отчёт / 10 USD → Warm → Hot → менеджер открывает карточку и берёт в работу.

Продуктовых 500 на этом пути нет. Inbox-блокер локального утра **на сайте закрыт**.

Перед показом всё ещё нужно: **`seed_demo_scenario`** (у `director@` 0 проектов) и **не кликать вкладки комнаты с карточки менеджера**.

---

## Находки

| Приоритет | Находка | Обход |
|-----------|---------|-------|
| **High** | `director@wowlance.demo` — «Проектов пока нет» | `python manage.py seed_demo_scenario` или показывать комнату нового директора |
| **High** | Часть POST (назначить тимлида, invite, GET мастера) отвечает **503**. Запрос может уже примениться: Артём Нестеров стал тимлидом, хотя клиент видел 503 | Повторить один раз, не жать дважды. gunicorn + SQLite |
| **High** | Карточка Hot рисует вкладки Обзор / Лиды / Материалы / Коммуникации и «← К задачам». Клик → 403 | Открыть из inbox, вкладки не трогать. Назад — «Горячие лиды» в шапке |
| **Medium** | POST `/leads/…/qualify/` от фрилансера не 403, а 200 + flash-ошибка. В UI формы нет, Hot не ставится | Не показывать. Hot ставит тимлид |
| **Gap** | Дашборд фрилансера без % принятых отчётов | Цифры на Обзоре комнаты + «Я заработал» |

---

## Что проверено

**Гость.** Лендинг, about/privacy/terms, логин, 404, logout GET = 405, CSS. Защищённые URL → `/login/`. Open redirect `next=` закрыт. `role=admin` / `role=manager` отклонены. «Применить архитектуру» → регистрация директора. WOW Talent: `?ref=wowtalent_demo` заполняет Ивана Иванова.

**Демо-логины** (`DemoPass123!`). Все четыре входят. Шапка по ролям верная. Каталог: директор и тимлид 200, фрилансер и менеджер 403. У Ивана нет «К каталогу». Inbox менеджера 200, старый «Пётр Лид» открывается (200).

**Золотой путь** на новых учётках (см. следы ниже): мастер cold_calling, одна кнопка «Оплатить и запустить», слоты после оплаты, статус Staffing. Назначение тимлида на клиенте 503, факт — Артём Нестеров. Дальше: фрилансер в комнате, ready, материал, две Jitsi, командный чат (XSS экранирован), DT скрыт от фрилансера, задача → в работу → отчёт со скрином → Quality Gate → approve → 10 USD → закрытие. Warm-лид, тимлид ставит Hot, менеджер открывает и берёт в работу. Вкладки комнаты у менеджера — 403.

---

## Следы на VPS

Создано **2 пользователя и 1 проект** (лимит 10 не нарушен). Пароль `DemoPass123!`.

| Что | Значение |
|------|----------|
| Директор | `qa.vps.dir.09022229@test.local` |
| Фрилансер | `qa.vps.fr.09022229@test.local` |
| Проект | «QA VPS Room 09022229» (`a8610feb-f46d-4634-b943-fdd736278e80`) |
| Тимлид комнаты | `artem.nesterov@wowlance.demo` |

В ту же комнату прогон по ошибке добавил существующего `alex.brown@wowlance.demo`. Можно оставить.

---

*Прогон 2026-09-02. Код по итогам не менялся. Вёрстку и HTMX глазами не смотрели.*

# Пример фрагмента nginx для HTTPS (подставьте свой сертификат).
# Для IP без домена Let's Encrypt обычно недоступен — нужен домен
# или самоподписанный сертификат (браузер покажет предупреждение).
#
# server {
#     listen 80;
#     server_name 195.19.209.121;
#     return 301 https://$host$request_uri;
# }
#
# server {
#     listen 443 ssl;
#     server_name 195.19.209.121;
#
#     ssl_certificate     /etc/ssl/certs/wowlance.crt;
#     ssl_certificate_key /etc/ssl/private/wowlance.key;
#
#     location /static/ {
#         alias /var/www/wowlance/staticfiles/;
#     }
#     location /media/ {
#         alias /var/www/wowlance/media/;
#     }
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
#
# В /etc/systemd/system/wowlance.service добавьте:
# Environment="USE_HTTPS=1"
# Environment="PUBLIC_SCHEME=https"
# Environment="PUBLIC_HOST=195.19.209.121"
